apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
spec:
  selector:
    app: scaletestapp
  type: NodePort
  ports:
    - port: 8085
      targetPort: 8080
      nodePort: 32123
      protocol: TCP